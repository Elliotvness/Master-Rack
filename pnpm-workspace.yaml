packages:
  - 'packages/*'
  - 'apps/*'

# esbuild is vitest's bundler and needs its postinstall to fetch its platform
# binary. Every other package is denied a build script by default.
allowBuilds:
  esbuild: true
