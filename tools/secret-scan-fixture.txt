# T-11 planted-credential fixture — deliberately fake, removed after the
# failing run was recorded. Matches the rms-planted-credential rule in
# .gitleaks.toml and nothing else.
RMSK_FAKECREDENTIAL123456